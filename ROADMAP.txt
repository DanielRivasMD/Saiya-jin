- remap:
	- use tab as primary zellij operator => tab control
	{
		- use zero as secondary zellij operator => pane & float control
		OR
		- use q as secondary zellij operator => pane control
		- use w as terciary zellij operator => float control
	}

	- use z & x as terminal program operators => helix, micro, lazygit
	- deprecate loptctl, loptcmd, lctlcmd
	- repurpose hyper as macosx control

- autogenerate katana config from saiyajin source => clojure -> edn & kbd
- create a function that autogenerates corresponding bindings
- babel micro & helix fully functional, lazygit, zellij; next broot
- system events & alfred hotkeys will remain annotated & render by `babel`, but ignore for semi-autogeneration
- sync browser hotkeys
- document terminal sequences
- document properly massive overhall
- bug: double Tq
- doc simple sequences

==================================================
src/apps/zoom.clj:; TODO: homogenize to format
src/apps/browser.clj:; TODO: update config
src/double/lctl.edn:; TODO: relocate
src/simple/rctl.clj:    ; TODO: annotate command line keys => history picker, etc
src/simple/lopt.clj:    ; TODO: launch float pane with editor (hx / mc); independent, or with reference
==================================================
